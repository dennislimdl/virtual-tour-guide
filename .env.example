# No API keys required for the map itself (uses Leaflet + OpenStreetMap).
#
# Required for the AI voice guide (narration + conversation) served by /api/guide.
# Get a free key (no credit card required) at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional: override the model used by the AI guide (defaults to gemini-2.5-flash).
# Use "gemini-2.5-flash-lite" for a higher free-tier daily request quota.
# GEMINI_MODEL=gemini-2.5-flash
